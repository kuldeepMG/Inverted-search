# Inverted-search
C-based Inverted Search application using Hash Tables and Linked Lists to build an inverted index for fast file searching. The project reads multiple text files, maps words to corresponding files, and enables efficient search operations using dynamic memory allocation and file handling concepts.
